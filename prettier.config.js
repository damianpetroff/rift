const config = {
  printWidth: 100,
  tabWidth: 2,
  semi: true,
  arrowParens: 'avoid',
  singleQuote: true,
  trailingComma: 'all',
  bracketSameLine: false,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
